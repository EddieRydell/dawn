// @generated by dawn-syntax-gen. Do not edit by hand.

use crate::diagnostic::{Diagnostic, DiagnosticKind};
use crate::generated::kind::SyntaxKind;

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LexToken {
    pub kind: SyntaxKind,
    pub range: std::ops::Range<usize>,
    pub text: String,
}

pub fn lex(source: &str) -> (Vec<LexToken>, Vec<Diagnostic>) {
    let mut tokens = Vec::new();
    let mut diagnostics = Vec::new();
    let mut cursor = 0;

    while cursor < source.len() {
        let rest = &source[cursor..];
        let start = cursor;
        let Some(ch) = rest.chars().next() else {
            break;
        };

        if ch.is_ascii_whitespace() {
            cursor += ch.len_utf8();
            while cursor < source.len() {
                let Some(next) = source[cursor..].chars().next() else {
                    break;
                };
                if !next.is_ascii_whitespace() {
                    break;
                }
                cursor += next.len_utf8();
            }
            tokens.push(LexToken {
                kind: SyntaxKind::Whitespace,
                range: start..cursor,
                text: source[start..cursor].to_string(),
            });
            continue;
        }

        if rest.starts_with("//") {
            cursor += rest.find('\n').unwrap_or(rest.len());
            tokens.push(LexToken {
                kind: SyntaxKind::LineComment,
                range: start..cursor,
                text: source[start..cursor].to_string(),
            });
            continue;
        }

        if rest.starts_with("/*") {
            if let Some(end) = rest.find("*/") {
                cursor += end + 2;
            } else {
                cursor = source.len();
                diagnostics.push(Diagnostic::new(
                    DiagnosticKind::UnterminatedBlockComment,
                    start..cursor,
                    "unterminated block comment",
                ));
            }
            tokens.push(LexToken {
                kind: SyntaxKind::BlockComment,
                range: start..cursor,
                text: source[start..cursor].to_string(),
            });
            continue;
        }

        if rest.starts_with('"') {
            cursor += 1;
            while cursor < source.len() && !source[cursor..].starts_with('"') {
                cursor += source[cursor..].chars().next().unwrap().len_utf8();
            }
            if cursor < source.len() {
                cursor += 1;
            }
            tokens.push(LexToken {
                kind: SyntaxKind::String,
                range: start..cursor,
                text: source[start..cursor].to_string(),
            });
            continue;
        }

        if rest.starts_with('#') {
            cursor += 1;
            while cursor < source.len() {
                let Some(next) = source[cursor..].chars().next() else {
                    break;
                };
                if !next.is_ascii_alphanumeric() {
                    break;
                }
                cursor += next.len_utf8();
            }
            let text = &source[start..cursor];
            let valid_len = text.len() == 4 || text.len() == 7;
            let valid_hex = text[1..].chars().all(|ch| ch.is_ascii_hexdigit());
            let kind = if valid_len && valid_hex {
                SyntaxKind::Color
            } else {
                diagnostics.push(Diagnostic::new(
                    DiagnosticKind::InvalidColor,
                    start..cursor,
                    "invalid color literal",
                ));
                SyntaxKind::InvalidColor
            };
            tokens.push(LexToken { kind, range: start..cursor, text: source[start..cursor].to_string() });
            continue;
        }

        if ch.is_ascii_digit() || (ch == '.' && rest[1..].chars().next().is_some_and(|next| next.is_ascii_digit())) {
            cursor += ch.len_utf8();
            while cursor < source.len() {
                let Some(next) = source[cursor..].chars().next() else {
                    break;
                };
                if !(next.is_ascii_digit() || next == '_') {
                    break;
                }
                cursor += next.len_utf8();
            }

            let mut kind = SyntaxKind::Int;
            if cursor < source.len() && source[cursor..].starts_with('.') {
                kind = SyntaxKind::Float;
                cursor += 1;
                while cursor < source.len() {
                    let Some(next) = source[cursor..].chars().next() else {
                        break;
                    };
                    if !(next.is_ascii_digit() || next == '_') {
                        break;
                    }
                    cursor += next.len_utf8();
                }
            }

            if cursor < source.len() {
                let next = source[cursor..].chars().next().unwrap();
                if next == 'e' || next == 'E' {
                    kind = SyntaxKind::Float;
                    cursor += 1;
                    if cursor < source.len() {
                        let sign = source[cursor..].chars().next().unwrap();
                        if sign == '+' || sign == '-' {
                            cursor += sign.len_utf8();
                        }
                    }
                    let digits_start = cursor;
                    while cursor < source.len() {
                        let Some(next) = source[cursor..].chars().next() else {
                            break;
                        };
                        if !(next.is_ascii_digit() || next == '_') {
                            break;
                        }
                        cursor += next.len_utf8();
                    }
                    if digits_start == cursor {
                        diagnostics.push(Diagnostic::new(
                            DiagnosticKind::InvalidToken,
                            start..cursor,
                            "invalid exponent",
                        ));
                    }
                }
            }

            if kind == SyntaxKind::Int && source[start..cursor].replace('_', "").parse::<i64>().is_err() {
                diagnostics.push(Diagnostic::new(
                    DiagnosticKind::InvalidInt,
                    start..cursor,
                    "invalid integer literal",
                ));
            }
            tokens.push(LexToken { kind, range: start..cursor, text: source[start..cursor].to_string() });
            continue;
        }

        if is_ident_start(ch) {
            cursor += ch.len_utf8();
            while cursor < source.len() {
                let Some(next) = source[cursor..].chars().next() else {
                    break;
                };
                if !is_ident_continue(next) {
                    break;
                }
                cursor += next.len_utf8();
            }
            tokens.push(LexToken {
                kind: keyword_kind(&source[start..cursor]).unwrap_or(SyntaxKind::Ident),
                range: start..cursor,
                text: source[start..cursor].to_string(),
            });
            continue;
        }

        if let Some((kind, len)) = punctuation_kind(rest) {
            cursor += len;
            tokens.push(LexToken { kind, range: start..cursor, text: source[start..cursor].to_string() });
            continue;
        }

        cursor += ch.len_utf8();
        diagnostics.push(Diagnostic::new(
            DiagnosticKind::InvalidToken,
            start..cursor,
            "invalid token",
        ));
    }

    (tokens, diagnostics)
}

fn is_ident_start(ch: char) -> bool {
    ch.is_ascii_alphabetic() || ch == '_'
}

fn is_ident_continue(ch: char) -> bool {
    is_ident_start(ch) || ch.is_ascii_digit()
}

fn keyword_kind(text: &str) -> Option<SyntaxKind> {
    Some(match text {
        "project" => SyntaxKind::ProjectKw,
        "display" => SyntaxKind::DisplayKw,
        "hardware" => SyntaxKind::HardwareKw,
        "layout" => SyntaxKind::LayoutKw,
        "patch" => SyntaxKind::PatchKw,
        "sequence" => SyntaxKind::SequenceKw,
        "curve" => SyntaxKind::CurveKw,
        "effect" => SyntaxKind::EffectKw,
        "event" => SyntaxKind::EventKw,
        "import" => SyntaxKind::ImportKw,
        "from" => SyntaxKind::FromKw,
        "version" => SyntaxKind::VersionKw,
        "label" => SyntaxKind::LabelKw,
        "description" => SyntaxKind::DescriptionKw,
        "controller" => SyntaxKind::ControllerKw,
        "input" => SyntaxKind::InputKw,
        "output" => SyntaxKind::OutputKw,
        "universe" => SyntaxKind::UniverseKw,
        "fixture" => SyntaxKind::FixtureKw,
        "group" => SyntaxKind::GroupKw,
        "members" => SyntaxKind::MembersKw,
        "filter" => SyntaxKind::FilterKw,
        "route" => SyntaxKind::RouteKw,
        "target" => SyntaxKind::TargetKw,
        "at" => SyntaxKind::AtKw,
        "for" => SyntaxKind::ForKw,
        "duration" => SyntaxKind::DurationKw,
        "frame_rate" => SyntaxKind::FrameRateKw,
        "audio" => SyntaxKind::AudioKw,
        "range" => SyntaxKind::RangeKw,
        "key" => SyntaxKind::KeyKw,
        "enum" => SyntaxKind::EnumKw,
        "flags" => SyntaxKind::FlagsKw,
        "param" => SyntaxKind::ParamKw,
        "spatial" => SyntaxKind::SpatialKw,
        "fn" => SyntaxKind::FnKw,
        "let" => SyntaxKind::LetKw,
        "if" => SyntaxKind::IfKw,
        "else" => SyntaxKind::ElseKw,
        "switch" => SyntaxKind::SwitchKw,
        "case" => SyntaxKind::CaseKw,
        "default" => SyntaxKind::DefaultKw,
        "true" => SyntaxKind::TrueKw,
        "false" => SyntaxKind::FalseKw,
        "float" => SyntaxKind::FloatKw,
        "int" => SyntaxKind::IntKw,
        "bool" => SyntaxKind::BoolKw,
        "color" => SyntaxKind::ColorKw,
        "gradient" => SyntaxKind::GradientKw,
        "path" => SyntaxKind::PathKw,
        "virtual_string" => SyntaxKind::VirtualStringKw,
        "ms" => SyntaxKind::MillisecondsKw,
        "s" => SyntaxKind::SecondsKw,
        "m" => SyntaxKind::MinutesKw,
        _ => return None,
    })
}

fn punctuation_kind(text: &str) -> Option<(SyntaxKind, usize)> {
    for (punctuation, result) in [
        ("**", Some((SyntaxKind::StarStar, 2))),
        ("==", Some((SyntaxKind::EqEq, 2))),
        ("!=", Some((SyntaxKind::BangEq, 2))),
        ("<=", Some((SyntaxKind::Le, 2))),
        (">=", Some((SyntaxKind::Ge, 2))),
        ("&&", Some((SyntaxKind::AndAnd, 2))),
        ("||", Some((SyntaxKind::OrOr, 2))),
        ("<<", Some((SyntaxKind::Shl, 2))),
        (">>", Some((SyntaxKind::Shr, 2))),
        ("{", Some((SyntaxKind::LBrace, 1))),
        ("}", Some((SyntaxKind::RBrace, 1))),
        ("[", Some((SyntaxKind::LBracket, 1))),
        ("]", Some((SyntaxKind::RBracket, 1))),
        ("(", Some((SyntaxKind::LParen, 1))),
        (")", Some((SyntaxKind::RParen, 1))),
        ("<", Some((SyntaxKind::Lt, 1))),
        (">", Some((SyntaxKind::Gt, 1))),
        (":", Some((SyntaxKind::Colon, 1))),
        (",", Some((SyntaxKind::Comma, 1))),
        (";", Some((SyntaxKind::Semicolon, 1))),
        (".", Some((SyntaxKind::Dot, 1))),
        ("=", Some((SyntaxKind::Eq, 1))),
        ("+", Some((SyntaxKind::Plus, 1))),
        ("-", Some((SyntaxKind::Minus, 1))),
        ("*", Some((SyntaxKind::Star, 1))),
        ("/", Some((SyntaxKind::Slash, 1))),
        ("%", Some((SyntaxKind::Percent, 1))),
        ("|", Some((SyntaxKind::Pipe, 1))),
        ("&", Some((SyntaxKind::Ampersand, 1))),
        ("^", Some((SyntaxKind::Caret, 1))),
        ("!", Some((SyntaxKind::Bang, 1))),
        ("?", Some((SyntaxKind::Question, 1))),
    ] {
        if text.starts_with(punctuation) {
            return result;
        }
    }
    None
}
