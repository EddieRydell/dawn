# Club Rig Dawn Sample

This sample uses Dawn's current data boundary:
project, display, layout, patch, and sequence manifests are YAML `.dawn` files;
sequence effects are authored inline in sequence YAML; effect sources remain `.effect.dawn`.

The effect language is intentionally kept as source text for this migration.
