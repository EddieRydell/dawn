# Club Rig Dawn Sample

This sample is intentionally written against the current Dawn syntax boundary:
each `.dawn` file has optional imports followed by one top-level document, and
document bodies are accepted as balanced blocks.

The nested body syntax is a sketch of the language direction, not a fully
validated semantic model yet.
